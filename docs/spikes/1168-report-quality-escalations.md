# Спайк #1168: сколько эскалаций порождает качество отчёта, а не качество кода

**Дата:** 2026-09-09. **Окно:** 90 дней. **Код:** `origin/develop` на `9149cf0`.
**Порог решения назван до подсчёта:** ниже 10% — «не строим».

**ВЫВОД: НЕ СТРОИМ.** Проверка размера порог проходит (нижняя граница 11.8%
против порога 10%), проверка реализуемости — нет: ступени, на которую
каскад эскалирует, не существует ни по профилю, ни по модели, а единственная
корзина, прошедшая порог, там где подъём вообще возможен, уже обслужена
лестницей #879 с 22.08. Задача на реализацию каскада не создаётся.

Каждое число ниже сопровождается вызовом или файлом, которым получено.
Числа без источника в этом документе не появляются; там, где выборки не
хватает, написано «недобор», а не подставлено значение.

---

## 0. Предмет спайка сдвинулся с даты постановки — это первый результат

Постановка написана 2026-09-05 и опирается на два утверждения, которые на
сегодняшнем `develop` уже неверны.

**«В `auto_verdict.py:264` задача уходит по условию `confirmed or unresolved
or incomplete`».** Условие переехало в `hub/services/gate_grounds.py`
(`unattended_blockers`, константа `UNATTENDED_BLOCKERS`) задачей #1170
(коммит `72f32d1`, 2026-09-05), чтобы стюард обязан был отказывать там же,
где отказывает автопилот. Смысл сохранён, адрес изменился.

**«Ни одна ступень лестницы не пробует переспросить САМО РЕВЬЮ сильнее».**
Неверно с 2026-08-22. `hub/services/review_dispatch.py::maybe_top_up_incomplete`
(#879, коммит `8021372`) покупает ТЯЖЁЛЫЙ профиль, когда дешёвый прогон сам
заявил, что не дочитал: это ровно «эскалация на более сильный инференс той же
работы после quality gate», то есть каскад HydraFusion, уже построенный для
одной из трёх корзин. У него есть свой потолок —
`REVIEW_LADDER_MAX_STEPS = 2` (`review_dispatch.py:911`).

Из трёх условий ухода одно (`incomplete`) уже имеет вторую попытку там, где
она физически возможна. Спайк, таким образом, спрашивает не «строить ли
каскад», а «достроить ли его до корзин `unresolved` и `no_data`».

**Ещё одно, важное для интерпретации окна.** Условие ухода появилось не 90
дней назад: `unresolved` вошёл в него задачей #745 (коммит `a7c0615`,
2026-08-20). Отчёты старше 20.08 этот уход породить не могли вовсе, поэтому
знаменатель «за 90 дней» завышен, а истинная доля — выше вычисленной ниже
нижней границы, не ниже.

---

## 1. Что удалось посчитать и что посчитать нельзя

Источник всех чисел этого раздела — один вызов
`hub_practice_metrics(since_days=90)` к проду `https://agenthai.ru`
(`instance=prod`, `server_id=vm-5c8197`), сделанный 2026-09-09.

### 1.1. Полное разложение по корзинам НЕ получено — и вот почему

AC-1 требует отнести каждый уход ровно к одной корзине так, чтобы сумма
корзин равнялась числу уходов. Это не сделано, и причина — не в нехватке
усилия, а в двух свойствах системы:

**(а) Уход по качеству отчёта молчалив.** В `hub/services/auto_verdict.py`
громкие основания (security, бюджет токенов, расхождение с соседом) зовут
`_escalate`, который пишет апдейт задачи И событие `verdict_escalated`.
Условие `grounds.unattended_blockers` — то самое, про которое спрашивает
спайк, — возвращает `False` без единой записи:

```python
    if grounds.unattended_blockers(
        confirmed, unresolved, bool(review.get("incomplete"))
    ):
        return False
```

Событий нет, апдейтов нет. Посчитать эти уходы по журналу событий нельзя ни
за какое окно. Это ровно тот исход, который постановка предусмотрела в
`assumptions` («если нет — это само по себе результат спайка»).

**(б) `unresolved` не агрегируется нигде.** Поле пишется в
`machine_review_intake.py` и хранится колонкой `machine_reviews.unresolved`,
но `hub_practice_metrics` его не считает ни в каком разрезе. Ни доля, ни
число отчётов с непустым `unresolved` из метрик не выводятся.

Остаётся единственный источник — построчный запрос к `machine_reviews`.
Он написан: `scripts/spike_1168_escalation_buckets.py`. Он реализует
заданный приоритет `no_data > incomplete > confirmed > unresolved`, печатает
отдельно число отчётов, где сработало больше одного условия, и отдельной
строкой — харнесс v7. Его функция отнесения проверена на наборе с нарочным
перекрытием условий:

```
$ python3 scripts/spike_1168_escalation_buckets.py --selftest
selftest: ок, случаев 6, из них с перекрытием 3
```

**Запустить его по проду из этой сессии не удалось.** Файл базы лежит в
`/var/lib/openclaw-hub/` (домашний каталог системного пользователя
`openclaw`, `getent passwd`), чтение оттуда требует прав, которых у
`user1` нет. Локальная база
`~/.local/state/haiplane-hub/hub.db` пуста (`machine_reviews`: 0 строк) —
запрос на ней отрабатывает без ошибок, то есть SQL и схема сходятся, но
чисел не даёт. Команда, которая закроет AC-1 целиком, одна:

```
python3 scripts/spike_1168_escalation_buckets.py --db <файл базы хаба> --since-days 90
```

### 1.2. Что посчитано: нижняя граница доли

Разложить нельзя — ограничить снизу можно, и этого хватает для сравнения с
порогом.

| Величина | Значение | Выборка / источник |
|---|---|---|
| отчётов машинного ревью в окне | 333 | `machine_reviews.reports_total`, 90 дней |
| из них харнесс v7 | 61 (из них `no_data` 60) | `by_harness`, `multi-agent-review` v7 |
| **основное число (всё, кроме v7)** | **272** | 333 − 61 |
| `no_data` всего | 62 | `machine_reviews.no_data_reports` |
| `no_data` вне v7 | **2** | 62 − 60 |
| `incomplete` всего | 44 | `by_profile.incomplete_runs`: deep 23 (из 112), «не заявлен» 13 (из 148), lite 8 (из 73) |
| `incomplete` с ЗАЯВЛЕННЫМ профилем | 31 | deep 23 + lite 8 |
| `incomplete` вне v7 | **≥ 30** | см. вывод ниже |
| `confirmed` (сколько ОТЧЁТОВ) | не выводится | метрики дают 352 находки, не число отчётов с находками |
| `unresolved` | **не измеряется вовсе** | недобор: поле не агрегируется |

**Почему `incomplete` вне v7 не меньше 30.** Отчёты v7 сидят в профиле «не
заявлен»: у этого профиля 61 `no_data`, у v7 их 60 — то есть максимум один
отчёт v7 имеет заявленный профиль. Значит из 31 отчёта с заявленным профилем
и `incomplete` к v7 относится не более одного.

**Нижняя граница доли уходов по качеству отчёта:**

```
(2 + 30) / 272 = 32 / 272 = 11.8%      против порога 10%
```

Граница безопасна в нужную сторону дважды: знаменатель взят по ОТЧЁТАМ, а
уходов не больше, чем отчётов; и окно 90 дней шире, чем возраст самого
условия (с 20.08). Числитель, наоборот, недосчитан на всю корзину
`unresolved`. Истинная доля выше 11.8%.

**Проверка размера порог проходит.** По одному этому числу решение было бы
«строим». Оно не принято — по разделам 2 и 3.

### 1.3. АC-2: харнесс v7 отдельной строкой

61 отчёт, из них 60 `no_data`. Это закрытый дефект #750 (`raw_count=0` при
непустом отчёте), то есть исправленное прошлое, а не текущее состояние. В
число, сравниваемое с порогом, они не входят — именно поэтому «26% no_data»
из постановки превращается в 2 отчёта из 272, то есть 0.7%. Самое крупное
число в пользу каскада действительно объяснялось закрытым багом; порог оно
не проходит и близко.

### 1.4. Изменила ли эскалация исход

`scope_in` просит долю уходов, где человек в итоге поставил APPROVED без
единой правки. Точного числа нет: диспозиций в окне ноль
(`dispositions.judged = 0` при `confirmed_unjudged = 135`), поэтому связать
уход с последующей правкой построчно нечем — **недобор**.

Верхняя граница есть: `review_outcomes` за окно — вердиктов 191, APPROVED
169, CHANGES_REQUESTED 22, то есть `changes_requested_rate = 0.115`
(выборка 155 задач). Значит эскалация меняла исход НЕ ЧАЩЕ чем в 11.5%
случаев, а в остальных 88.5% человек в итоге одобрял. Это верхняя граница по
всем вердиктам, не только по уходам, и как самостоятельное число не
годится — но направление она задаёт однозначно.

---

## 2. AC-3: цена одной второй попытки

Источник: `by_profile.provider_tokens_per_run` того же вызова.

| Профиль | Цена одного прогона | Выборка (оплаченных прогонов) |
|---|---|---|
| deep | 2 150 344 provider-токенов | 92 |
| lite | 1 530 508 provider-токенов | 68 |
| «не заявлен» | счёта нет вовсе (`billed_runs = 0`) | недобор |

Живая сверка: отчёт #323 по задаче #1171, поставленный сегодня, — профиль
deep, модель grok-4.6, `provider_tokens = 2 442 629`
(`hub_get_review_brief(1171)`, 2026-09-09). Средняя по 92 прогонам и
единичное сегодняшнее наблюдение сходятся.

**Месячная оценка при текущей частоте.** Неудовлетворённых существующей
лестницей уходов по `incomplete` в окне 23 (профиль deep: подниматься выше
некуда) — это 23 / 3 ≈ **7.7 вторых попыток в месяц**, каждая по цене deep:

```
7.7 × 2 150 344 ≈ 16.6 млн provider-токенов в месяц
```

Для масштаба: всего в окне израсходовано 301 906 189 provider-токенов
(`provider_tokens_total`, 160 отчётов со счётом), то есть ≈ 100.6 млн в
месяц. Достройка каскада только на эту корзину — **плюс ≈ 16% к счёту за
ревью**. Корзину `unresolved` в оценку включить нельзя: её размер не
измерен (недобор), и месячное число для неё было бы выдумкой.

---

## 3. AC-5: на какую ступень каскад эскалирует — проверка реализуемости

Каскад — это подъём на более сильную ступень ТОЙ ЖЕ работы. Ступеней две:
профиль и модель. Обе проверены.

**Профиль: верх занят.** `maybe_top_up_incomplete` покупает deep только
дешёвому прогону, и говорит об этом прямо
(`hub/services/review_dispatch.py:973-983`):

> «Неполный отчёт, добор не положен: профиль … — выше дешёвого подниматься
> некуда. Решение за человеком (#879).»

23 из 30 измеренных `incomplete` — это уже deep. Профильной ступени над ними
нет по построению.

**Модель: пул сужен до трёх имён, и верхнее уже используется по умолчанию.**
`_REVIEW_MODEL_PREFERENCES` (`review_dispatch.py:58`) — это
`grok-4.6, grok-4.5, composer-2.5`, с комментарием, что список сужен до
подписки 2026-08-28 (#1036): всё, что вне её, отказывает при создании
агента с `HTTP 400 usage_limit_exceeded`. `grok-4.6` — первое предпочтение,
то есть модель, которой ревью и так делается: сегодняшний прогон по #1171
идёт именно на ней. Над ней в списке ничего нет.

**Что проверено живым вызовом, а что нет — честно.**

- Живой `GET /v1/models` из этой сессии сделать НЕ УДАЛОСЬ: ключ провайдера
  (`CURSOR_API_KEY`) лежит в systemd-drop-in с правами 600 на прод-хосте и
  этой сессии недоступен. **AC-5 в части «ответ живого вызова по каждому
  кандидату» закрыт не полностью, и выдавать за него чужую запись я не
  буду.** Команда, которая его закрывает, — `hub.integrations.cursor_cloud.list_models()`
  с прод-окружением.
- Живое наблюдение из хаба, которое у меня есть: за 90 дней стюард не вынес
  НИ ОДНОГО суждения — `steward_shadow` даёт `both_approve: 0`,
  `escalated: 0`, `unpaired: 0` и отказ `no_sample` («суждений нет вовсе»).
  Стюард по умолчанию запускается на `gpt-5.3-codex`
  (`config.STEWARD_MODEL`) — то есть единственная более сильная модель,
  названная в коде, за всё окно не дала ни одного результата, который можно
  было бы предъявить как доказательство её доступности.

**Найдено противоречие в записях о подписке — его надо разрешить отдельно, а
не решать здесь.** `#1036` (28.08) вычеркнул `gpt-5.3-codex` и
`gemini-3.1-pro` как «гарантированный отказ», а `config.py:168-184` (#1182,
06.09) держит `gpt-5.3-codex` основным судьёй и `gemini-3.1-pro` запасным, с
комментарием «имена проверены у провайдера через list_models 06.09.2026».
Либо подписка изменилась между 28.08 и 06.09, либо один из двух списков
описывает несуществующую возможность. Ноль суждений стюарда за окно —
косвенный довод за второе, но это довод, а не измерение.

---

## 4. AC-4: вывод

**НЕ СТРОИМ.**

Сравнение с порогом показано явно: измеренная нижняя граница доли уходов по
качеству отчёта — **11.8%** (32 из 272 отчётов вне v7), порог — **10%**.
Размерная проверка пройдена. Решение принято не по ней, и вот по чему:

1. **Ступени, на которую эскалировать, нет.** 23 из 30 измеренных уходов по
   `incomplete` — уже deep; выше профиля нет. Пул моделей сужен до трёх, и
   верхняя (`grok-4.6`) уже стоит по умолчанию. Постановка называет это
   самостоятельным основанием: «отсутствие доступной модели — самостоятельное
   основание для "не строим"» (риск `breaking_change`).
2. **Там, где подъём возможен, каскад уже есть.** #879 покупает deep дешёвому
   прогону с 22.08, с потолком в 2 шага. Вторая такая же лестница была бы
   вторым описанием одного правила — ровно то, чего #1170 избегал, разводя
   auto_verdict и стюарда по одному списку.
3. **Корзина, ради которой стоило бы достраивать (`unresolved`), не
   измерена.** Строить механизм под неизмеренную корзину — это писать код под
   аналогию, что спайк и должен был предотвратить.

Задача на реализацию каскада не создаётся.

---

## 5. Отдельный вопрос: отличим ли круг от полезной работы

Вопрос встал 09.09 на живом материале: задачи закрывали все находки отчёта,
вносили НАСТОЯЩИЕ правки и получали новый слой настоящих находок.

**Существующие потолки этого не видят — измерено, а не предположено.**

- Потолок «повтор без изменения» (#1152) ключуется на вершину ветки:
  повторный прогон не ставится, только когда `submission_sha` тот же
  (`review_dispatch.py:1285-1295`: «Пересдача подняла поколение, но дифф не
  изменился»). Круг с настоящими правками проходит его каждый раз по
  построению.
- `MAX_REVIEW_CYCLES = 3` (`config.py:65`) считает циклы ЧЕЛОВЕЧЕСКИХ
  вердиктов CHANGES_REQUESTED. Наблюдение по #1171 сегодня
  (`hub_get_review_brief(1171)`): `submission_generation = 3` при
  `review_cycle = 0`. Круг прошёл три поколения, а счётчик, который должен
  был бы его поймать, стоит на нуле.
- `REVIEW_LADDER_MAX_STEPS = 2` ограничивает прогоны ВНУТРИ одного поколения
  и обнуляется каждой пересдачей.

**Ни один потолок не считает поколения.** Это и есть ответ: круг с
настоящими правками сегодня невидим по построению, а не по недосмотру.

**Признак, по которому он всё же отличим в уже сохранённых данных.** Он не
требует ни новой инструментации, ни второго прогона — только чтения
`machine_reviews` по поколениям одной задачи:

- **Траектория `confirmed` против `unresolved`.** Полезная работа сводит к
  нулю обе секции. Круг сводит к нулю `confirmed` (автор чинит то, с чем
  верификаторы согласились) и НЕ сводит `unresolved` — секцию, по которой
  согласия не было и не появляется. Наблюдение 09.09, выборка 3 задачи —
  **недобор для правила, достаточно для гипотезы**: #1171 confirmed 3→0 при
  unresolved 5→5; #1208 confirmed 3→0 при unresolved 0→4; #1169 — пять
  подтверждённых закрыты и доказаны мутациями, следующий отчёт принёс ещё
  пять.
- **Предмет повторного ревью — дельта, а не весь диф** (#874; видно в
  сводке сдачи #1171: «Предмет ревью: дельта к поколению #2»). Значит
  находка на поколении ≥2 — это находка ПРО ПРАВКУ. Полезная работа даёт
  убывающую дельту и убывающий урожай; круг даёт постоянный. Категория для
  этого в хабе уже есть и уже непуста: `fix-induced-regression` — 12 находок
  на 6 задачах за окно (`recurring_categories`).

Скрипт спайка печатает эту траекторию по каждой задаче с двумя и более
поколениями и считает, у скольких задач `confirmed` падает, а `unresolved`
не падает.

**Что из этого следует и чего не следует.** Потолок другого рода —
считающий ПОКОЛЕНИЯ, а не повторы одной вершины, и печатающий траекторию
вместо тишины — из данных следует. Но это не предмет этого спайка: он
измеряет, а не перепроектирует, и менять `auto_verdict`, пороги или контракт
стюарда ему запрещено (`scope_out`). **Предложение: завести отдельную задачу
на потолок по поколениям.** Здесь она не создаётся.

---

## 6. Что осталось неизмеренным

| Что | Почему | Чем закрывается |
|---|---|---|
| Полное разложение по четырём корзинам (AC-1) | уход молчалив (нет события), `unresolved` не агрегируется, файл базы недоступен этой сессии | `scripts/spike_1168_escalation_buckets.py --db <база> --since-days 90` |
| Размер корзины `unresolved` | то же | то же |
| Живой `GET /v1/models` по каждому кандидату (AC-5) | ключ провайдера — на прод-хосте, права 600 | `list_models()` из `hub/integrations/cursor_cloud.py` с прод-окружением |
| Доля «APPROVED без единой правки» | диспозиций в окне ноль (`judged = 0` при 135 неразобранных) | разбор находок (#1171) |
| Признак круга как правило, а не гипотеза | выборка 3 задачи | траекторный раздел того же скрипта по полному архиву |

**Рекомендация по постоянному агрегату** (постановка просит только
рекомендацию): долю отчётов с непустым `unresolved` в `hub_practice_metrics`
закрепить стоит — сегодня единственная секция отчёта, которая уводит сдачу с
детерминированного пути, не видна ни в одной метрике, и любой следующий
разговор о ней начнётся с той же невозможности посчитать.

---

## Условие пересмотра

Прежний вывод недействителен, если меняется контракт отчёта машинного ревью
или состав условий в `unattended_blockers` — распределение считается заново.
Отдельно: если подписка провайдера расширится и над `grok-4.6` появится
доступная модель, довод 1 раздела 4 отпадает, и вопрос возвращается — но уже
с измеренной корзиной `unresolved`, а не без неё.
