# Спайк #1402: находка с падающим тестом от ревьюера

**Дата:** 2026-09-25. **Родитель:** эпик #994. **Код:** `origin/develop` на `043e210`.
**Порог назван до подсчёта (AC-4):** «вводим», только если тестом подкреплено
≥ 20% находок **и** ≥ 70% приложенных тестов падают на sha находки по причине
из её формулировки. Иначе «не вводим», и следующий кандидат — тест из фикса автора.

**ВЫВОД: ВВОДИМ В ФОРМАТ ОТЧЁТА** (условно по цене, см. §5).

- Тест приложен к **17 из 24** находок (**70.8%**, порог 20%).
- На sha находки упали **17 из 17** тестов. Причина падения совпала с формулировкой
  находки в **17 из 17** буквально и в **16 из 17** строго (**94%**, порог 70%):
  тест #18 проверяет форму кода через AST, а не поведение.
- «Неисполнимо» — **7 из 24** (**29.2%**), считается отдельной строкой.
  «Не проверено: окружение» — **0**.
- На sha фикса прошли **9 из 15** тестов у исправленных находок (60%). Ещё 5 падают и там,
  потому что фикс был сделан в другой форме, чем записал в тесте ревьюер. Один тест
  проходит наполовину.
- precision_exec (падает на sha находки **и** проходит на sha фикса): **9 из 24** находок
  (37.5%), или 9 из 15 исправленных находок с тестом.
- **Цена в provider-токенах НЕ названа.** Для прогонов через `cursor-agent` счёта в хабе
  нет. В §5 перечислены id сессий и окна UTC всех 23 прогонов для сверки с панелью
  Cursor, там же базовая линия обычного прогона из `provider_tokens`. Самоотчёт клиента
  или харнесса в цену не подставлен. AC-3 закрыт только наполовину.

---

## 1. Метод

**Источник находок.** Прод, только через `~/bin/prod-db-ro.sh`, одна SELECT за вызов,
колонки названы явно. Использованы таблицы `machine_reviews`, `submissions`,
`finding_outcomes`, `tasks` (id) и `review_dispatches` (разрешение владельца от 25.09).

- sha находки берётся из `submissions.sha` по паре (task_id, generation) отчёта.
  `finding_outcomes.submission_generation` всегда равен поколению отчёта: 348 из 348 строк.
- sha фикса — первое следующее поколение той же задачи, у которого `sha` отличается от
  sha находки.
- Оба коммита должны существовать в `origin` (`git cat-file -e`). Из 344 сопоставленных
  находок с исходом у 52 в `origin` нет коммита находки, ещё у 5 нет коммита фикса.
  Вероятная причина — наследие зеркала mrPDA.

**Выборка.** Только находки, у которых записан исход автора, потому что без исхода
нет sha фикса. Пул и сколько взято из каждого слоя:

| слой | пул (оба sha есть) | взято |
|---|---:|---:|
| confirmed / fixed | 130 | 10 |
| confirmed / false_positive | 1 | 1 |
| confirmed / wont_fix | 2 | 1 |
| unresolved / real_fixed | 130 | 9 |
| unresolved / not_a_defect | 9 | 3 |

Внутри слоя порядок детерминированный: `sha1(finding_outcomes.id)`, берутся первые N.
Находки false_positive, wont_fix и not_a_defect добавлены как контроль: тест, который
падает и на них, подтверждает поведение, а не дефект (см. §6).

**Ревьюер.** Локальный `cursor-agent` на подписке владельца (разрешение от 25.09),
модель `cursor-grok-4.6-high`. В `--list-models` её имя «Grok 4.6»: голого id
`grok-4.6` у CLI нет. Это то же семейство, что `grok-4.6` у ревьюера хаба; все 24
исходные находки выборки сделал именно `grok-4.6`.

Режим запуска: `--mode ask` (только чтение), `--print --output-format stream-json`.
MCP-серверы не одобрялись (`--approve-mcps` не передавался). В потоках встречаются только
`readToolCall`, `grepToolCall` и `globToolCall`: ни записи, ни shell, ни MCP. Записей
в хаб нет.

Для каждой находки создаётся одноразовый `git worktree --detach` на её sha в `$TMPDIR`.
В промте: заголовок, severity, category, место и `detail` находки (у unresolved вместо
`detail` поле `why`). Требование — один pytest-файл, падающий на этом sha именно из-за
описанного дефекта, либо ответ «НЕИСПОЛНИМО: причина». Исход автора ревьюеру не
сообщался. Выполнять код ревьюер не мог, поэтому свой тест он не прогонял.

**Прогон тестов.** Тесты запускал исполнитель спайка, а не ревьюер, той же командой,
что CI: `PYTHONDONTWRITEBYTECODE=1 uv run pytest -q -p no:cacheprovider -rA <файл>`.

- Файл кладётся в `tests/` worktree на sha находки, затем в worktree на sha фикса, и
  после прогона удаляется.
- Перед прогоном worktree проверяется на чистоту: `git status --porcelain` пуст во всех 34.
- Засчитывается только падение на `assert`, текст которого совпадает с формулировкой
  находки. Падение на импорте или коллекции не засчитывается (таких не было: все 17
  дают `failed`, `error` нет ни одного).

**Отличие от предложенного механизма.** В предложении тест гоняет CI на закреплённом
sha через `.github/actions/hub-ci-report`. В спайке тесты гонялись локально (macOS)
той же командой. Путь сообщения CI не проверялся: это следующая ступень, а не
предмет спайка.

Тесты ревьюера лежат дословно в `docs/spikes/1402-finding-tests/finding_NN.py`. Каталог
вне `testpaths`, поэтому набор хаба их не собирает. Воспроизвести:

```
git worktree add --detach /tmp/f <sha>
cp docs/spikes/1402-finding-tests/finding_NN.py /tmp/f/tests/test_finding_NN.py
cd /tmp/f && uv run pytest -q -rA tests/test_finding_NN.py
```

## 2. Выборка

Ниже fo = `finding_outcomes.id`, rev = `machine_reviews.id`. sha сокращены до 8 знаков.

| # | fo | rev | задача/пок. | вид / исход автора | sha находки | sha фикса | профиль | заголовок (сокращён) |
|---|---|---|---|---|---|---|---|---|
| 1 | 122 | 318 | #1167/5 | confirmed / fixed | 649b2e23 | c691236c | deep | Подмена базы названа в пакете и не доезжает до отчёта реплея |
| 2 | 154 | 330 | #1161/1 | confirmed / fixed | c37b1031 | e5a1e01b | lite | Битый JSON findings выдаётся за «замечаний не было» |
| 3 | 17 | 237 | #1085/1 | confirmed / fixed | 55fe8b5e | b1e3749c | deep | Отказ от finding_uid на unresolved не покрыт приёмом отчёта |
| 4 | 327 | 535 | #1356/1 | confirmed / fixed | 9b5da438 | b57ee329 | lite | Возврат из fix_requested ломается об активный job_id |
| 5 | 157 | 337 | #1168/1 | confirmed / fixed | 1f047cb7 | cbfe8992 | lite | Вывод «ступени нет» при незакрытом AC-5 |
| 6 | 260 | 442 | #1242/1 | confirmed / fixed | c88c98e9 | 1ab4b8dc | deep | Цепочка paid→429→paid считается двумя ступенями |
| 7 | 235 | 346 | #1239/1 | confirmed / fixed | bbf1ac19 | 6d41c56a | deep | Пустой дифф без workspace остаётся измеренной поверхностью |
| 8 | 126 | 327 | #1169/3 | confirmed / fixed | a1237570 | 75059002 | deep | Сид case 2 не фиксирует отсутствие события |
| 9 | 85 | 312 | #1167/3 | confirmed / fixed | 5be805e9 | 729f0090 | deep | Исторический пакет судит по сегодняшней карточке |
| 10 | 150 | 331 | #1167/6 | confirmed / fixed | c691236c | 2bc0861f | deep | Трёхточечный дифф по имени базы пустеет после мержа |
| 11 | 213 | 329 | #1206/2 | confirmed / false_positive | b347efc0 | da517136 | lite | Живой round-trip маркера не записан — AC-1, AC-3 |
| 12 | 340 | 552 | #1246/3 | confirmed / wont_fix | 69376e5e | 4448ff28 | lite | «expected rc=0, got rc=1» считается заявлением о зелёном |
| 13 | 127 | 327 | #1169/3 | unresolved / real_fixed | a1237570 | 75059002 | deep | Ветка «Сработавших правил нет» не зафиксирована |
| 14 | 161 | 333 | #1155/2 | unresolved / real_fixed | 55906319 | 6c23d361 | deep | Pair/claimed auto_review=false done теряет предупреждения |
| 15 | 338 | 550 | #1246/2 | unresolved / real_fixed | f26d455a | 69376e5e | deep | Классификатор rc=0 принимает отрицание |
| 16 | 175 | 345 | #1233/1 | unresolved / real_fixed | 05280492 | 378d6eed | deep | classify_conflict: add/add целого файла = tail_additions |
| 17 | 293 | 480 | #1332/1 | unresolved / real_fixed | eb7ae61b | 1dfb1d33 | deep | CancelledError оставляет ребёнка host-profile живым |
| 18 | 337 | 541 | #1246/1 | unresolved / real_fixed | 0062b368 | f26d455a | deep | AC-тесты не диспатчат ревью, сами собирают reviewer_prompt |
| 19 | 263 | 443 | #1242/1 | unresolved / real_fixed | c88c98e9 | 1ab4b8dc | deep | asked>retried навсегда останавливает переспрос |
| 20 | 93 | 314 | #1167/4 | unresolved / real_fixed | 729f0090 | 649b2e23 | deep | Исторический дифф молча берёт сегодняшнюю базу |
| 21 | 28 | 260 | #1194/1 | unresolved / real_fixed | d5197a24 | 4f4ea50f | deep | Повторный redeem обнуляет живой токен |
| 22 | 210 | 355 | #1168/2 | unresolved / not_a_defect | cbfe8992 | ebc7eb1e | lite | Запись живой проверки #58 не читалась |
| 23 | 211 | 355 | #1168/2 | unresolved / not_a_defect | cbfe8992 | ebc7eb1e | lite | Драфт #1243 не подтверждён |
| 24 | 218 | 401 | #1214/2 | unresolved / not_a_defect | 87dd99d0 | f417c60e | deep | Стюард считает доставку только по pipeline_merges |

## 3. Результаты по находкам (AC-1, AC-2)

Обозначения в колонках: **ответ** — что вернул ревьюер; **на sha находки** — исход
прогона (✗ = упал, ✓ = прошёл); **причина = находка** — совпадает ли упавший assert
с формулировкой находки; **на sha фикса** — исход там же.

| # | ответ | на sha находки | причина = находка | на sha фикса | комментарий |
|---|---|---|---|---|---|
| 1 | тест (2 функции) | ✗ 2 failed | да: «сегодняшн…» нет в `render_report`; заметка пишет «не названа» | ✗ 2 failed | фикс эскалирует сдачу (`escalated=1`), а не печатает счётчик; тест закрепил форму фикса, которую автор не выбрал |
| 2 | тест | ✗ 1 failed | да: 409 содержит «без единого замечания» при битом JSON | ✓ passed | подтверждён полностью |
| 3 | неисполнимо | — | — | — | пробел покрытия: отказ `refuse_supplied_uid` в коде уже есть, assert прошёл бы |
| 4 | тест | ✗ 1 failed | да: `return_to_work` → 409 на живом `job_id` | ✗ failed | фикс добавил явный `abandon_active_job=true`; вызов по умолчанию всё ещё 409 |
| 5 | неисполнимо | — | — | — | находка про вывод спайк-документа и живой `GET /v1/models` |
| 6 | тест | ✗ 1 failed | да: `count_review_dispatches` = 2 вместо 1 | ✓ passed | подтверждён полностью |
| 7 | тест | ✗ 1 failed | да: `state=present within_declared=True` без workspace | ✓ passed | подтверждён полностью |
| 8 | неисполнимо | — | — | — | пробел покрытия (category=tests): поведение уже верное, нужен мутант, а не падающий тест |
| 9 | тест | ✗ 1 failed | да: `hub/db.py` исчез из undeclared из-за accept_areas gen-2 | ✓ passed | подтверждён полностью |
| 10 | тест | ✗ 1 failed | да: пустой дифф после мержа выдан за измеренный | ✓ passed | подтверждён полностью |
| 11 | неисполнимо | — | — | — | состояние карточки и живой ответ провайдера. Контроль: автор назвал false_positive |
| 12 | тест (2 функции) | ✗ 2 failed | да: «expected rc=0, got rc=1» попадает в `_claims_green` | неприменимо (wont_fix); ✗ AttributeError — API переименован | поведение подтверждено, автор отказался чинить. Совпадает со строкой 15 на её sha фикса |
| 13 | неисполнимо | — | — | — | пробел покрытия: шаблон уже рисует текст |
| 14 | тест | ✗ 1 failed | да: done закрывает сабтаск, не назвав находку | ✓ passed | подтверждён полностью |
| 15 | тест (2 функции) | ✗ 2 failed | да: `not rc=0`, `did not exit code 0` и др. стали `author_claims` | ✗ 1 failed / ✓ 1 passed | фикс убрал 3 формы из 4; осталась «expected rc=0, got rc=1» — ровно находка 12 (wont_fix) |
| 16 | тест | ✗ 1 failed | да: `'tail_additions' == 'unresolvable'` | ✓ passed | подтверждён полностью |
| 17 | тест | ✗ 1 failed | да: маркер дописан после отмены | ✓ passed | подтверждён полностью |
| 18 | тест | ✗ 1 failed | буквально да, по сути нет: AST-проверка, что `_surfaces` не зовёт `prepare_review_order` | ✗ failed | структурный тест: проверяет форму кода, а не поведение. Фикс (`e440982`, `1a2f935`) `_surfaces` не трогает |
| 19 | тест | ✗ 1 failed | да: алерт «повторять вслепую нельзя» после отказа конфига | ✗ failed | фикс (`1ab4b8d`) убрал формулировку «вслепую», но переспрос не вернул; тест требует и того и другого |
| 20 | тест | ✗ 1 failed | да: three-dot взял `main-today` | ✗ failed | фикс (sha находки 1) называет подмену в заметке, но не запрещает её; это и есть находка 1 |
| 21 | тест | ✗ 1 failed | да: файл токена стал пустым после 401 | ✓ passed | подтверждён полностью |
| 22 | неисполнимо | — | — | — | процесс: чтение чужой карточки. Контроль: not_a_defect |
| 23 | неисполнимо | — | — | — | процесс: драфт в хабе. Контроль: not_a_defect |
| 24 | тест | ✗ 1 failed | да: стюард пишет «не доставлено 1», API — `delivery_path=unknown` | ✗ failed | поведение подтверждено, но автор назвал его not_a_defect. Тест доказывает поведение, а не дефект |

### Итоговые доли

| строка | число | доля |
|---|---:|---:|
| находок в выборке | 24 | 100% |
| тест приложен | 17 | 70.8% |
| «неисполнимо» (отдельная строка) | 7 | 29.2% |
| — из них пробел покрытия (category=tests) | 3 | 12.5% |
| — из них процесс, документ или внешняя система | 4 | 16.7% |
| «не проверено: окружение» | 0 | 0% |
| приложенные тесты, упавшие на sha находки | 17 из 17 | 100% |
| — причина совпала буквально | 17 из 17 | 100% |
| — причина совпала строго (без структурного #18) | 16 из 17 | 94.1% |
| тесты у исправленных находок (fixed / real_fixed), прошедшие на sha фикса | 9 из 15 | 60% |
| — упали и на sha фикса: фикс в другой форме | 5 из 15 | 33% |
| — прошли частично | 1 из 15 | 7% |
| **precision_exec** (падает на sha находки и проходит на sha фикса) | 9 из 24 | 37.5% |

Контроль: 5 находок, которые автор не стал чинить или отверг (#11 false_positive,
#12 wont_fix, #22, #23 и #24 not_a_defect).

- 3 из 5 ревьюер сам назвал неисполнимыми: #11, #22, #23. Все три про процесс или
  карточку, а не про код.
- Для #12 (wont_fix) и #24 (not_a_defect) тест приложен и **упал**. Значит, падающий
  тест подтверждает, что заявленное **поведение** есть, но не подтверждает, что это
  **дефект**. Спор «дефект ли это» тест не решает, и ставить ему это в заслугу нельзя.

## 4. Прогоны ревьюера и бюджет

**Счётчик: 23 из 25 прогонов израсходовано.** Считается каждый старт `cursor-agent`,
включая оборванные.

| прогон | находки | начало (UTC) | конец (UTC) | исход | session_id |
|---|---|---|---|---|---|
| 1 | 1 | 2026-09-25T05:01:41Z | 05:08:30Z | ответ | 19d26e0a-f44c-4a52-92d0-b289b4fdc34b |
| 2 | 2 | 05:08:52Z | 05:50:21Z | оборван, ответа нет | b40e465f-39da-48b6-b23e-907dad66c116 |
| 3 | 3 | 05:08:52Z | 05:50:21Z | оборван, ответа нет | 0121e7ce-b451-4c6d-98a5-84d37c2e4bfe |
| 4 | 4 | 05:08:52Z | 05:50:21Z | оборван, ответа нет | abdf6ff8-ea87-432d-8059-bfe32f1fa786 |
| 5 | 5 | 05:08:52Z | 05:10:03Z | ответ | 3de6b1ac-ec4b-4079-ac7e-9103f299a402 |
| 6 | 6 | 05:10:03Z | 05:50:21Z | оборван, ответа нет | 376daff6-73f8-432e-9a74-5d0c4d6cad44 |
| 7 | 7 | 06:50:12Z | 07:11:01Z | снят по потолку, ответа нет | 08b0ba40-0d2d-476f-8497-7b59ca6f9a67 |
| 8 | 8 | 06:50:12Z | 07:11:01Z | снят по потолку, ответа нет | a7f8f1a9-e36a-4106-b328-0468a5e7121c |
| 9 | 9 | 06:50:12Z | 07:11:01Z | снят по потолку, ответа нет | 1d685d11-8822-4205-8132-ed3a5ce00e94 |
| 10 | 10 | 06:50:12Z | 07:11:01Z | снят по потолку, ответа нет | 93746f5c-64ad-4f5b-ae5e-92cafef6ee3e |
| 11 | 11 | 07:11:02Z | 07:11:22Z | снят исполнителем | c1b05bbf-dea9-41b1-a185-d25ea7c5ea80 |
| 12 | 12 | 07:11:02Z | 07:11:28Z | снят исполнителем | c92ac58a-dfb3-485c-8181-cff5285d59f3 |
| 13 | 13 | 07:11:02Z | 07:11:29Z | снят исполнителем | f2329dd5-d843-4f16-bb38-b1a1ef57e40f |
| 14 | 14 | 07:11:02Z | 07:11:29Z | снят исполнителем | f16917e3-3ad1-4c98-bf6f-5c9937c7c4bc |
| r15 | 8, 13 | 07:12:46Z | 07:15:51Z | ответ | f7e3c87d-0862-43ba-81fa-37fbe1a235c2 |
| r16 | 2, 3, 4 | 07:16:10Z | 07:21:06Z | ответ | 9c72949b-2528-4664-b984-07f047bec284 |
| r17 | 6, 19 | 07:21:29Z | 07:40:44Z | снят: петля переподключений | ba9e60a7-ae38-404a-b1bb-c6ba7dd18789 |
| r18 | 22, 23, 11 | 07:53:00Z | 07:54:16Z | ответ | 686028be-881f-4581-a6c1-5851ace7d686 |
| r19 | 6, 19, 7 | 07:54:31Z | 07:59:14Z | ответ | a0204166-1c61-4a5d-9e28-0365a8091ca7 |
| r20 | 9, 10, 12 | 07:59:27Z | 08:05:35Z | ответ | cd64589f-0cc4-4252-a59f-e5005aa8e2ab |
| r21 | 14, 15, 16 | 08:05:57Z | 08:10:40Z | ответ | 7c8c69a6-fe73-4ac8-91c4-329b9c9d0a79 |
| r22 | 17, 18, 20 | 08:10:57Z | 08:17:16Z | ответ | 999bd9ef-094f-46f8-8852-14a6b9af5830 |
| r23 | 21, 24 | 08:17:25Z | 08:22:59Z | ответ | 3dfe9d09-54a8-4438-8df3-107689a664d9 |

Все даты — 2026-09-25.

**Что пошло не так с бюджетом.** Прогоны 2–4, 6 и 7–10 шли по четыре параллельно, и
все застряли в петле `connection: reconnecting` / `retry: resuming`. Первое
переподключение случилось примерно через 100 с после старта, дальше backoff растёт до
минут. 2–4 и 6 оборвались вместе с сессией исполнителя. 7–10 сняты по потолку. 11–14
сняты вручную через несколько секунд после старта.

Одиночные прогоны (1, 5, r15, r16, r18–r23) закончились за 1–7 минут. Исключение — r17:
он застрял в той же петле и шёл один, но на аккаунте в это время работали
`cursor-agent` других сессий владельца. Так что параллелизм вероятная, но не доказанная
причина.

Оборванные прогоны учтены в бюджете, но **ответов не дали**. Поэтому 22 находки
разложены на 9 прогонов по 2–3 находки в каждом. Как это сказывается на выводе,
разобрано в §6.

## 5. Цена (AC-3) — НЕ НАЗВАНА, ждёт панель Cursor

**Базовая линия: обычный прогон ревьюера хаба в provider-токенах.** Источник —
`prod-db-ro.sh`, колонка `provider_tokens`, 25.09.

| источник | профиль | окно | n | среднее provider_tokens |
|---|---|---|---:|---:|
| `review_dispatches` | deep | 30 дней | 250 | 2 266 746 |
| `review_dispatches` | lite | 30 дней | 145 | 1 632 516 |
| `review_dispatches` | deep | всё время | 247* | 2 260 606 |
| `review_dispatches` | lite | всё время | 141* | 1 641 265 |
| `machine_reviews` | deep | всё время | 226 | 2 294 560 |
| `machine_reviews` | lite | всё время | 150 | 1 629 800 |

\* Запросы «всё время» сняты раньше запросов «30 дней», поэтому n в них меньше:
между снятиями на проде добавились новые dispatch.

**Прогоны спайка.** Для `cursor-agent` счёта в хабе нет. Цена берётся только из панели
Cursor по окнам и session_id из §4. Самоотчёт клиента (`usage` в потоке) и самоотчёт
харнесса как цену не использую.

**Формула, которую нужно заполнить из панели:**

- `C_run` — токены провайдера успешных прогонов 1, 5, r15, r16, r18–r23. Они дали ответы
  на все 24 находки.
- `C_waste` — токены провайдера оборванных прогонов 2–4, 6–14 и r17. Это цена
  нестабильного канала, а не механизма.
- Добавочная цена находки с тестом ≈ `C_run / 24`, а на приложенный тест —
  `C_run / 17`. Это **верхняя** оценка. В спайке каждый прогон заново читал код ради
  уже готовой находки. В боевом отчёте тест пишется в том же прогоне, который находку
  нашёл, и код уже в контексте.
- Месячная оценка: в постановке заложено ≈ 118 отчётов в месяц. Замер за последние
  30 дней: `machine_reviews` — 471 отчёт и 904 находки (1.92 на отчёт), `review_dispatches`
  — 412 dispatch по 178 задачам. При ≈ 118 отчётах это ≈ 227 находок в месяц, из них
  при доле 70.8% ≈ 160 с тестом. Итого `160 × C_run/17` в месяц, или `640 × C_run/17`
  при замеренных 904 находках.

Пока `C_run` не снят, вывод «вводим» опирается только на пороги AC-4 и **условен по
цене**. Если `C_run/24` окажется сопоставимым с целым прогоном ревьюера
(≈ 1.6–2.3 млн токенов), требование теста удвоит цену ревью. В этом случае вывод
нужно пересмотреть, и порог пересмотра по цене должен назвать владелец.

## 6. Вывод и критика

**Вводим в формат отчёта:** поле «тест, падающий на sha находки» или явное
«неисполнимо: причина». Оба порога AC-4 пройдены с запасом: 70.8% против 20% и 94%
против 70%.

Что вывод **не** говорит, и почему числа могут быть завышены:

1. **Смещение выборки.** Брались только находки с исходом автора и с обоими sha
   в `origin`. 19 из 24 взятых автор починил (fixed / real_fixed), то есть находки
   заведомо конкретные. 139 неразобранных находок (#1168) могут оказаться
   размытее, и доля «неисполнимо» на них будет выше.
2. **Падающий тест подтверждает поведение, а не дефект.** #12 (wont_fix) и #24
   (not_a_defect) упали так же уверенно, как исправленные. 100% падений на sha
   находки — это «ревьюер верно прочёл код», а не «ревьюер прав, что это ошибка».
   Для precision сильнее сигнал «проходит на sha фикса», а он даёт только 60%, потому что…
3. **Тест закрепляет форму фикса, которую выбрал ревьюер.** В 5 из 15 случаев
   (#1, #4, #18, #19, #20) автор починил иначе: эскалация вместо печати, явный флаг
   вместо тихой отмены, переименование вместо поведения. Тест остался красным, хотя
   исход «fixed» автор записал честно. Значит, «не проходит на sha фикса» нельзя
   считать ложной находкой. precision_exec = 37.5% — **нижняя** граница.
4. **Структурные тесты.** #18 проверяет AST исходника, а не поведение. Такой тест
   «падает по причине находки» всегда, по построению. В формате нужно запретить
   inspect/AST-тесты или считать их отдельно, иначе строгая доля превращается в
   буквальную.
5. **Пробелы покрытия неисполнимы по определению.** 3 из 7 «неисполнимо» — находки
   category=tests: дефект в том, что теста нет, а поведение верное. Их подтверждает
   мутант (путь мутаций в CI уже есть: `.github/actions/hub-ci-report`), а не падающий
   тест. В формате для них нужна третья ветка, «мутант», а не «неисполнимо».
6. **Условия спайка отличаются от боевых.** Ревьюеру давали готовую находку, и он не
   мог запускать код. В бою тест пишется в том же прогоне и может быть прогнан, так что
   качество, скорее всего, выше, а цена ниже. Пакетирование по 2–3 находки на прогон
   (вынуждено бюджетом, §4) сравнить честно нельзя. В одиночных и парных прогонах тест
   приложен к 3 из 6 находок, в тройных — к 14 из 18. Но состав разный: три
   coverage-находки и одна про документ попали в одиночные и парные. Выборка для
   такого сравнения мала.
7. **CI не гонял тесты.** Команда та же, но ОС другая (macOS против Linux в CI), и путь
   сообщения `hub-ci-report` не проверен. Внедрение обязано начаться с этого.
8. **Цены нет** (§5). Вывод условен, пока `C_run` не снят из панели.

**Условие пересмотра.** Вывод пересматривается в пользу теста из фикса автора
(следующий кандидат T3), если выполняется хотя бы одно:

- на первых 30 боевых отчётах с полем доля приложенных тестов упадёт ниже 20%;
- доля тестов, падающих в CI на sha находки по причине находки, упадёт ниже 70%;
- `C_run/24` превысит порог цены, который назовёт владелец.
