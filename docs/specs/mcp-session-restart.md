# «session expired» после релиза: вызов MCP выполнен, ответ потерян

Спайк #1363, 24.09.2026. Код хаба не менялся. Лечение заведено задачей #1364
(DoR пройден).

## Коротко

- Сессии MCP живут в словаре процесса. Рестарт при релизе их теряет.
- Первый вызов каждой клиентской сессии после рестарта получает от сервера
  404 `Session not found`. Инструмент при этом **не выполняется**.
- Claude Code сам переподключается и **один раз повторяет** вызов. Этот повтор
  доходит до сервера и **пишет**.
- Параллельно в клиенте работает фоновый цикл переподключения. Примерно через
  секунду он закрывает соединение, по которому идёт повтор. Агент получает
  `MCP server "openclaw-hub" session expired` на уже выполненной записи.
- За 25.08–24.09 в логах клиента 281 такой автоповтор. 101 из них закончился
  ошибкой при записи, которая прошла. У `hub_submit_for_review` это 50 из 60:
  она медленная (5–15 с) и почти всегда проигрывает гонку.
- Сам клиент дублей не делает. Дубли делал агент, который вручную повторял
  вызов после ошибки: 12 наблюдённых двойных записей, все за 26.08–01.09.
- Рекомендация: перевести транспорт в stateless-режим (#1364). Нет сессии —
  нет 404, нет пути восстановления в клиенте и нет гонки. Ключ идемпотентности
  (#1059) остаётся нужным для настоящих обрывов сети.

## Что наблюдено, а что выведено

| Утверждение | Статус | Откуда |
|---|---|---|
| Сессии хранятся только в памяти процесса | наблюдено в коде | SDK, раздел 1 |
| Устаревший `Mcp-Session-Id` получает 404, инструмент не выполняется, записей 0 | наблюдено опытом | локальный хаб, раздел 2 |
| Claude Code повторяет вызов сам после 404 | наблюдено | логи клиента, раздел 3 |
| Повтор выполняется на сервере, а агент получает ошибку | наблюдено | логи клиента + `task_updates` + `mcp_call_events`, раздел 3 |
| Ошибку вызывает гонка двух переподключений внутри клиента | выведено из порядка строк лога; код клиента не читался (закрытая сборка) | раздел 3 |
| Двойные записи были, их делал агент ручным повтором | наблюдено | раздел 4 |
| Клиент повторяет вызов и при обрыве посреди выполнения (без 404) | **не наблюдено**: все 281 автоповтор шли после 404 | раздел 3 |
| В stateless-режиме вызов после рестарта проходит сразу, одна запись | наблюдено опытом с Python-клиентом SDK | раздел 2 |
| Claude Code, Cursor и Codex работают со stateless-сервером | **не проверено** | #1364 AC-3 |
| Идентичность вызывающего (bearer, principal) доходит до инструмента в stateless | **не проверено** | #1364 AC-2 |

Пробел: `journalctl` на хосте не читался. Время рестартов взято из
постановки #1363 и сверено с коммитами релизов в `git log origin/main` и с
логом клиента (строка `Failed to open SSE stream: Bad Gateway` в момент
рестарта). Логов сервера о пришедших запросах нет. Их заменяет телеметрия
`mcp_call_events`: она пишется **после** выполнения инструмента
(`hub/mcp_server.py:125-150`), поэтому 404 на уровне транспорта в неё не
попадает, а выполненный вызов попадает всегда.

## 1. Где живут сессии

- `hub/mcp_server.py:153`: `InstrumentedFastMCP(...)` создаётся без
  `stateless_http`. По умолчанию в mcp 1.28.1 это `False`, то есть режим со
  сессиями.
- `hub/app.py:240`: `mcp_server.streamable_http_app()` создаёт
  `StreamableHTTPSessionManager`. Строка `hub/app.py:302` запускает его внутри
  lifespan приложения, `hub/app.py:3532` монтирует его в корень.
- `.venv/.../mcp/server/streamable_http_manager.py:90`: сессии хранятся в
  `self._server_instances: dict[str, StreamableHTTPServerTransport]`. Это
  живые anyio-задачи в памяти процесса. Внешнего хранилища SDK не
  предусматривает, `session_idle_timeout` по умолчанию `None`.
- `streamable_http_manager.py:336-341`: неизвестный id получает 404 и
  JSON-RPC `-32600 Session not found`. Ответ уходит **до** всякого разбора
  вызова.
- `deploy/remote-deploy.sh:42`: каждый релиз делает
  `systemctl restart haiplane-hub`, и процесс начинает с пустым словарём.
  Сервис работает одним процессом uvicorn (`hub/app.py:3541`), поэтому
  воркеров, которые не знают сессию друг друга, нет. Причина только в
  рестарте.

## 2. Опыт на локальном хабе (AC-1)

Скрипт: временный хаб на 127.0.0.1 с пустой базой. Клиент на Python SDK
(`mcp.client.streamable_http`). Шаги: открыть сессию, записать A через
`hub_task_update`, SIGTERM и новый старт процесса, послать `tools/call` с
**прежним** `Mcp-Session-Id`, посчитать строки в `task_updates`, открыть
новую сессию и повторить. Второй прогон — тот же хаб, где `stateless_http`
выставлен до построения приложения (лабораторный запуск, в код не входит).

| Шаг | stateful (как на проде) | stateless |
|---|---|---|
| запись A в живой сессии | ok, 1 запись | ok, 1 запись |
| вызов B со старым id после рестарта | **HTTP 404** `Session not found` | **HTTP 200**, результат |
| записей B после этого вызова | **0** | **1** |
| повтор B в новой сессии | ok, записей B = 1 | ok, записей B = 2 (повтор неидемпотентной записи) |

Вывод: после рестарта сервер сам ничего не выполняет по устаревшей сессии.
Записать может только повторный вызов в новой сессии. Stateless убирает 404
целиком. Последняя строка ещё раз показывает, что `hub_task_update` при
повторе пишет второй раз.

## 3. Что делает клиент (Claude Code)

Источник: логи MCP-клиента
`~/Library/Caches/claude-cli-nodejs/-Users-denispukinov-openclaw-hub-standalone/mcp-logs-openclaw-hub/*.jsonl`,
436 файлов, 25.08–24.09. Клиент `claude-code/2.1.280`.

Сдача #1334, 23.09 (UTC). Рестарт в 21:26:38, релиз `0dff64d`.

```
21:26:38.105  Failed to open SSE stream: Bad Gateway          <- рестарт
21:26:39.714  Maximum reconnection attempts (2) exceeded.
21:26:39.714  SSE GET-stream reconnection exhausted; leaving transport up (POST still works)
              ... 2.5 минуты тишины, клиент держит старый id ...
21:29:07.450  Calling MCP tool: hub_submit_for_review          <- вызов агента
21:29:07.765  Error POSTing to endpoint: {"code":-32600,"message":"Session not found"}
21:29:07.765  MCP session expired (server no longer recognizes session ID), triggering reconnection
21:29:07.769  http transport closed — reconnecting (attempt 1/5)   <- фоновый цикл A
21:29:07.772  Tool 'hub_submit_for_review' failed after 0s: Connection closed
21:29:07.773  Retrying tool 'hub_submit_for_review' after session recovery   <- путь повтора B
21:29:08.101  Connection error: The operation was aborted.     <- A и B мешают друг другу
21:29:08.127  Reconnect attempt 1 did not connect (failed); next in 1000ms
21:29:08.307  Calling MCP tool: hub_submit_for_review          <- повтор B ушёл на сервер
21:29:09.128  Reconnect attempt 2/5                            <- цикл A заменяет соединение
21:29:09.128  Tool 'hub_submit_for_review' failed after 0s: Connection closed   -> агенту ошибка
```

Сервер в то же время:

- `task_updates` id 7671, `Submitted for review (submission #4)`, 21:29:10.
- `mcp_call_events` `hub_submit_for_review`, task 1334, status ok,
  latency 6496 мс, записано в 21:29:15. Вызов начался около 21:29:08.6,
  это и есть повтор B.

Сдача #1337, 20:50:04. Та же последовательность, только старая сессия
пережила рестарт 20:27 и была использована через 23 минуты. Запись:
`task_updates` 7615 в 20:50:08, телеметрия 20:50:11. Ошибка у агента в
20:50:07. Два других случая 23.09 — #1337 в 14:57:40 и #1333 в 15:37:56 —
идут по тому же шаблону, их записи 7571 и 7598.

Следствия:

1. «Первые минуты после рестарта» — неточная формулировка. Ломается **первый
   вызов каждой клиентской сессии** после рестарта, сколько бы времени ни
   прошло: 2.5 мин, 23 мин, 67 мин (случай 14:57 после рестарта в 13:50).
2. Клиент видит, что GET-поток умер, но переинициализацию не делает.
   Устаревший id живёт до следующего POST.
3. Повтор вызывает сам клиент. Ошибка возникает, когда его же фоновый цикл
   переподключения закрывает соединение с повтором. Это гонка в клиенте, а не
   на сервере. Вывод сделан по порядку строк: исходников сборки нет.

Сводка по всем логам за 25.08–24.09:

| | всего | повтор завершился успехом | повтор закрыт «Connection closed» |
|---|---|---|---|
| все автоповторы | 281 | 174 | 101 |
| из них пишущие инструменты | 174 | 98 | 70 (+6 содержательных отказов) |
| из них `hub_submit_for_review` | 60 | 10 | **50** |

Все 281 автоповтор шли после 404 `Session not found`: проверено по 25
строкам лога перед каждым повтором. Случаев «вызов выполнился, соединение
оборвалось, клиент повторил» в данных нет. Для всех 70 закрытых пишущих
повторов в `mcp_call_events` есть выполненный вызов того же инструмента в
пределах 60 с. Запрос: CTE из 174 событий лога, `LEFT JOIN mcp_call_events`
по `tool` и окну `[ts, ts+60s]`. Окно не различает авторов, поэтому это
верхняя граница, но нулей среди 70 нет.

## 4. Двойные записи на проде (AC-2)

Запрос (только чтение, `prod-db-ro.sh`): пары `task_updates` у одной задачи с
одинаковым `kind` в пределах 90 с, где обе записи — «Submitted for review»
или совпадают по тексту, с 21.08.

Связаны с разобранным механизмом (в логе клиента в ту же минуту есть 404,
автоповтор, «Connection closed», а затем ручной вызов агента):

| задача | что задвоилось | время (UTC) |
|---|---|---|
| #962 | `report_done`, два одинаковых `done` | 26.08 06:19:14 / 06:19:25 |
| #969 | `hub_task_update`, одинаковый текст | 26.08 21:00:48 / 21:00:49 |
| #1065 | сдача #1 и #2 | 30.08 00:00:49 / 00:01:22 |
| #1069 | `report_done`, два одинаковых `done` | 30.08 18:06:23 / 18:06:31 |
| #1079 | сдача #1 и #2 | 30.08 18:29:34 / 18:29:43 |
| #1076 | сдача #1 и #2 | 30.08 18:51:33 / 18:51:56 |
| #1114 | сдача #1 и #2 | 31.08 21:09:35 / 21:09:46 |
| #1067 | сдача #1 и #2 | 31.08 21:15:53 / 21:16:27 |
| #1106 | сдача #2 и #3 | 31.08 22:41:07 / 22:41:30 |
| #1147 | сдача #1 и #2 | 01.09 22:11:56 / 22:12:31 |
| #1146 | сдача #1 и #2 | 01.09 22:50:14 / 22:50:55 |

Три вызова в каждом случае. Пример #1079: 18:29:30 — 404, 18:29:32 —
автоповтор (выполнился, ответ потерян), 18:29:41 — ручной вызов агента
(выполнился второй раз).

Не связаны с механизмом: #1172, сдачи #6 и #7 13.09, две независимые сессии,
разобрано в #1265. Сюда же относятся повторы `Plan:` у #945, #949, #953 и
#955 24–25.08 (в логах клиента автоповтора нет) и двойные `Review verdict`
от admin/policy. Это другие причины, в спайк они не входят.

После 01.09 дублей сдач нет, хотя «закрытые» повторы сдач продолжались (23.09
их четыре). Защищают две вещи. Первая — дисциплина агента: после ошибки
сначала `hub_task_status`. Вторая — #1265: повтор того же sha из `review` не
открывает новое поколение. Устройство системы от этого не меняется: ошибка
по-прежнему ложная, а защита держится на агенте.

## 5. Какие вызовы опасно повторять

Опасность одна для всех: агент получает ошибку на записи, которая уже
прошла, и повторяет её вручную.

| инструмент | повтор после прошедшей записи | основание |
|---|---|---|
| `hub_task_update` | **вторая запись в ленте** | опыт раздела 2; #969 |
| `hub_report_done` | **второй `done` в ленте**; с `pending_report` возможно второе поколение (#1059) | #962, #1069; код `pending_report` не проверялся |
| `hub_submit_for_review` | тот же sha из `review` — без нового поколения (#1265); после движения ветки или из другого статуса — новое поколение | `hub/services/lifecycle.py:2400-2435`; до #1265 — 9 дублей |
| `hub_propose_task` | **второй черновик**: ключа нет | сигнатура в `hub/mcp_server.py:2827` |
| `hub_create_task` | безопасно, если передан `client_request_id` | `hub/mcp_server.py:376-386` |
| `hub_session_register`, `hub_upsert_acceptance_criterion` | идемпотентны по контракту | `hub/mcp_server.py:387-405` |
| чтения (`hub_task_status` и др.) | безопасны | — |

## 6. Варианты лечения и их цена

| вариант | что убирает | цена | риск |
|---|---|---|---|
| **A. Stateless-транспорт** (`stateless_http=True`) | 404 и весь путь восстановления в клиенте: ложной ошибки нет, соблазна повторять нет | S: одна настройка + тесты идентичности + живая проверка клиентов | contextvar'ы bearer/identity могут не дойти до инструмента: задача менеджера создаётся в lifespan. Клиент без `Mcp-Session-Id` не проверен. Хаб не использует серверных уведомлений, сэмплинга и элиситации (греп `ctx.`, `report_progress`, `elicit` пуст), так что функций не теряет |
| B. Сессии вне памяти | 404 после рестарта | L: SDK не даёт подключить хранилище, сессия — это живая задача. Нужен свой менеджер, который «воскрешает» неизвестный id без `initialize` | ломает контракт протокола. `event_store` в SDK даёт только повтор событий потока, а не сессию |
| C. Мягкий рестарт без обрыва (два процесса, передача сокета) | обрыв на время релиза | L: сессии всё равно в памяти старого процесса, новый их не знает. Помогает, только если старый дослуживает клиентов, а это для долгих сессий бессмысленно | сложный деплой ради того, что A делает одной строкой |
| D. Ключ идемпотентности на пишущих вызовах (#1059) | вред от ручного повтора, в том числе при настоящих обрывах сети | M: задача готова, DoR пройден | ложную ошибку не убирает. Защищает только вызовы, которые передают ключ |
| E. Клиент: сообщить о гонке в Claude Code; агентам — «после ошибки сначала статус» | гонку у источника / дубли дисциплиной | вне репозитория / уже в памяти агентов | не в наших руках; дисциплина не является свойством системы |

## 7. Рекомендация

1. **A — заведено #1364** (S, DoR пройден). Ошибка возникает, только если сервер
   отвечает 404 на устаревшую сессию. Без сессий такого ответа нет. Хаб
   сессиями ни для чего не пользуется.
2. **D — #1059 оставить в очереди.** Stateless не спасает от настоящего обрыва
   посреди долгой сдачи: рестарт во время выполнения, сеть. Посылка #1059
   «чинить транспорт не надо» этим спайком уточняется: транспорт чинить
   **можно**, и это убирает основной поток ложных ошибок. Ключ нужен для
   остатка.
3. **E — сообщить о гонке в Claude Code** (гонка фонового переподключения и
   `Retrying tool … after session recovery`). Приложить последовательность из
   раздела 3.

## 8. Что проверить следующим шагом

- #1364 AC-2: в stateless-режиме `principal_id` вызывающего попадает в
  `mcp_call_events`, а инструмент ходит в REST с тем же bearer. Проверять на
  настоящем приложении с токеном, не через `.fn`.
- #1364 AC-3: живой Claude Code на локальном хабе переживает рестарт. В логе
  клиента нет `Session not found` и `Retrying tool`, запись одна. То же для
  Cursor и Codex.
- После выката: в `mcp-logs-openclaw-hub` за неделю ноль строк
  `Session not found`. Это метрика исхода #1364.
- Не наблюдено и стоит отдельного опыта: что получает клиент, если рестарт
  приходится **на выполнение** долгой записи (uvicorn по SIGTERM, закрытие
  lifespan менеджера), и повторяет ли тогда Claude Code вызов сам. Если
  повторяет, это дубль без участия агента, и тогда #1059 срочнее.
