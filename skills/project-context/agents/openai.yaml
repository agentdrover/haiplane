interface:
  display_name: "Project Context"
  short_description: "Fast map for standalone Haiplane Hub"
  default_prompt: "Use $project-context to map the relevant files, invariants, and tests for this Haiplane Hub task."

policy:
  allow_implicit_invocation: true
